# API Keys
# These keys are optional and serve as default values.
# Clients can provide their own API keys via X-Api-Key header.
# If a client provides a key, it will override these defaults.

# Anthropic API Key (optional)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI API Key (optional)
OPENAI_API_KEY=sk-...

# Google Gemini API Key (optional)
GEMINI_API_KEY=...

# Optional: Path to configuration file (default: config.toml)
CONFIG_PATH=
